// Exercise 3: Verify if a number is positive
// Step 1: Declare a variable and assign any number to it.
// Step 2: Declare a boolean variable and check if the first variable is greater than 0.
// Step 3: Log the result.

const number: number = -592;
const isPositive: boolean = number > 0;
console.log(`Is ${number} a positive number? ${isPositive}`);
const isNegative: boolean = number < 0;
console.log(`Is ${number} a negative number? ${isNegative}`);
