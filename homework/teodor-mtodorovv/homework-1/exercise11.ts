// Exercise 11: BigInt addition
// Step 1: Create two bigint numbers.
// Step 2: Add the two bigint numbers together.
// Step 3: Log the result.

// Step 1: Create two bigint numbers.
let firstNumber: bigint = 200000000000000000000n;
let secondNumber: bigint = 70000000000000000n;

// Step 2: Add the two bigint numbers together.
const sumNumbers: bigint = firstNumber + secondNumber;

// Step 3: Log the result.
console.log('The sum of 2 bigint numbers is', sumNumbers);
